name: cooldown_button
publish_to: none
version: 0.5.1

environment:
  sdk: ">=3.13.3 <4.0.0"
  flutter: ">=3.47.4"

dependencies:
  flutter:
    sdk: flutter
  badges: 3.2.0
  flutter_countdown_timer: 4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
flutter: